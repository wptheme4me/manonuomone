extends ./default.pug

block main
 section
  .r
   .c0
    
    form#forma(action="#")
     h1 #{title}
     label(for="username") Username/E-mail
     input(type="text" id="username")
     label(for="password") Password
     input(type="text" id="password")
     a(href="your-profile.html", title="title").button Log-in
     a(href="index.html", title="title").button Cancel

block footer
 

